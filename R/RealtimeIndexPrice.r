# Intrinio API
#
# Welcome to the Intrinio API! Through our Financial Data Marketplace, we offer a wide selection of financial data feed APIs sourced by our own proprietary processes as well as from many data vendors. For a complete API request / response reference please view the [Intrinio API documentation](https://docs.intrinio.com/documentation/api_v2). If you need additional help in using the API, please visit the [Intrinio website](https://intrinio.com) and click on the chat icon in the lower right corner.
#
# OpenAPI spec version: 2.107.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' RealtimeIndexPrice Class
#'
#' @field last_price 
#' @field last_time 
#' @field index 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
RealtimeIndexPrice <- R6::R6Class(
  'RealtimeIndexPrice',
  public = list(
    `last_price` = NA,
    `last_time` = NA,
    `index` = NA,
    initialize = function(`last_price`, `last_time`, `index`){
      if (!missing(`last_price`)) {
        self$`last_price` <- `last_price`
      }
      if (!missing(`last_time`)) {
        self$`last_time` <- `last_time`
      }
      if (!missing(`index`)) {
        self$`index` <- `index`
      }
    },
    toJSON = function() {
      RealtimeIndexPriceObject <- list()
      if (!is.null(self$`last_price`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`last_price`) && ((length(self$`last_price`) == 0) || ((length(self$`last_price`) != 0 && R6::is.R6(self$`last_price`[[1]]))))) {
          RealtimeIndexPriceObject[['last_price']] <- lapply(self$`last_price`, function(x) x$toJSON())
        } else {
          RealtimeIndexPriceObject[['last_price']] <- jsonlite::toJSON(self$`last_price`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`last_time`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`last_time`) && ((length(self$`last_time`) == 0) || ((length(self$`last_time`) != 0 && R6::is.R6(self$`last_time`[[1]]))))) {
          RealtimeIndexPriceObject[['last_time']] <- lapply(self$`last_time`, function(x) x$toJSON())
        } else {
          RealtimeIndexPriceObject[['last_time']] <- jsonlite::toJSON(self$`last_time`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`index`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`index`) && ((length(self$`index`) == 0) || ((length(self$`index`) != 0 && R6::is.R6(self$`index`[[1]]))))) {
          RealtimeIndexPriceObject[['index']] <- lapply(self$`index`, function(x) x$toJSON())
        } else {
          RealtimeIndexPriceObject[['index']] <- jsonlite::toJSON(self$`index`, auto_unbox = TRUE)
        }
      }

      RealtimeIndexPriceObject
    },
    fromJSON = function(RealtimeIndexPriceJson) {
      RealtimeIndexPriceObject <- jsonlite::fromJSON(RealtimeIndexPriceJson)
      if (!is.null(RealtimeIndexPriceObject$`last_price`)) {
        self$`last_price` <- RealtimeIndexPriceObject$`last_price`
      }
      if (!is.null(RealtimeIndexPriceObject$`last_time`)) {
        self$`last_time` <- RealtimeIndexPriceObject$`last_time`
      }
      if (!is.null(RealtimeIndexPriceObject$`index`)) {
        self$`index` <- RealtimeIndexPriceObject$`index`
      }
    },
    toJSONString = function() {
      jsonlite::toJSON(self$toJSON(), auto_unbox = TRUE, pretty = TRUE)
    },
    fromJSONString = function(RealtimeIndexPriceJson) {
      RealtimeIndexPriceObject <- jsonlite::fromJSON(RealtimeIndexPriceJson, simplifyDataFrame = FALSE)
      self$setFromList(RealtimeIndexPriceObject)
    },
    setFromList = function(listObject) {
      if (!is.null(listObject$`last_price`)) {
        self$`last_price` <- listObject$`last_price`
      }
      else {
        self$`last_price` <- NA 
      }





      if (!is.null(listObject$`last_time`)) {
        self$`last_time` <- as.POSIXct(listObject$`last_time`, tz = "GMT", "%Y-%m-%dT%H:%M:%OS")
      }
      else {
        self$`last_time` <- NA 
      }











      self$`index` <- RealtimeIndexPriceIndex$new()
      self$`index`$setFromList(listObject$`index`)

    },
    getAsList = function() {
      listObject = list()
      listObject[["last_price"]] <- self$`last_price`


      listObject[["last_time"]] <- self$`last_time`


        




      index_list <- self$`index`$getAsList()
      for (x in names(index_list)) {
        listObject[[paste("index_",x, sep = "")]] <- self$`index`[[x]]
      }
        
      return(listObject)
    }
  )
)
