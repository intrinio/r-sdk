# Intrinio API
#
# Welcome to the Intrinio API! Through our Financial Data Marketplace, we offer a wide selection of financial data feed APIs sourced by our own proprietary processes as well as from many data vendors. For a complete API request / response reference please view the [Intrinio API documentation](https://docs.intrinio.com/documentation/api_v2). If you need additional help in using the API, please visit the [Intrinio website](https://intrinio.com) and click on the chat icon in the lower right corner.
#
# OpenAPI spec version: 2.107.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' Beta Class
#'
#' @field period_type 
#' @field one_year_beta 
#' @field three_year_beta 
#' @field five_year_beta 
#' @field seven_year_beta 
#' @field ten_year_beta 
#' @field security 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Beta <- R6::R6Class(
  'Beta',
  public = list(
    `period_type` = NA,
    `one_year_beta` = NA,
    `three_year_beta` = NA,
    `five_year_beta` = NA,
    `seven_year_beta` = NA,
    `ten_year_beta` = NA,
    `security` = NA,
    initialize = function(`period_type`, `one_year_beta`, `three_year_beta`, `five_year_beta`, `seven_year_beta`, `ten_year_beta`, `security`){
      if (!missing(`period_type`)) {
        self$`period_type` <- `period_type`
      }
      if (!missing(`one_year_beta`)) {
        self$`one_year_beta` <- `one_year_beta`
      }
      if (!missing(`three_year_beta`)) {
        self$`three_year_beta` <- `three_year_beta`
      }
      if (!missing(`five_year_beta`)) {
        self$`five_year_beta` <- `five_year_beta`
      }
      if (!missing(`seven_year_beta`)) {
        self$`seven_year_beta` <- `seven_year_beta`
      }
      if (!missing(`ten_year_beta`)) {
        self$`ten_year_beta` <- `ten_year_beta`
      }
      if (!missing(`security`)) {
        self$`security` <- `security`
      }
    },
    toJSON = function() {
      BetaObject <- list()
      if (!is.null(self$`period_type`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`period_type`) && ((length(self$`period_type`) == 0) || ((length(self$`period_type`) != 0 && R6::is.R6(self$`period_type`[[1]]))))) {
          BetaObject[['period_type']] <- lapply(self$`period_type`, function(x) x$toJSON())
        } else {
          BetaObject[['period_type']] <- jsonlite::toJSON(self$`period_type`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`one_year_beta`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`one_year_beta`) && ((length(self$`one_year_beta`) == 0) || ((length(self$`one_year_beta`) != 0 && R6::is.R6(self$`one_year_beta`[[1]]))))) {
          BetaObject[['one_year_beta']] <- lapply(self$`one_year_beta`, function(x) x$toJSON())
        } else {
          BetaObject[['one_year_beta']] <- jsonlite::toJSON(self$`one_year_beta`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`three_year_beta`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`three_year_beta`) && ((length(self$`three_year_beta`) == 0) || ((length(self$`three_year_beta`) != 0 && R6::is.R6(self$`three_year_beta`[[1]]))))) {
          BetaObject[['three_year_beta']] <- lapply(self$`three_year_beta`, function(x) x$toJSON())
        } else {
          BetaObject[['three_year_beta']] <- jsonlite::toJSON(self$`three_year_beta`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`five_year_beta`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`five_year_beta`) && ((length(self$`five_year_beta`) == 0) || ((length(self$`five_year_beta`) != 0 && R6::is.R6(self$`five_year_beta`[[1]]))))) {
          BetaObject[['five_year_beta']] <- lapply(self$`five_year_beta`, function(x) x$toJSON())
        } else {
          BetaObject[['five_year_beta']] <- jsonlite::toJSON(self$`five_year_beta`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`seven_year_beta`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`seven_year_beta`) && ((length(self$`seven_year_beta`) == 0) || ((length(self$`seven_year_beta`) != 0 && R6::is.R6(self$`seven_year_beta`[[1]]))))) {
          BetaObject[['seven_year_beta']] <- lapply(self$`seven_year_beta`, function(x) x$toJSON())
        } else {
          BetaObject[['seven_year_beta']] <- jsonlite::toJSON(self$`seven_year_beta`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`ten_year_beta`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`ten_year_beta`) && ((length(self$`ten_year_beta`) == 0) || ((length(self$`ten_year_beta`) != 0 && R6::is.R6(self$`ten_year_beta`[[1]]))))) {
          BetaObject[['ten_year_beta']] <- lapply(self$`ten_year_beta`, function(x) x$toJSON())
        } else {
          BetaObject[['ten_year_beta']] <- jsonlite::toJSON(self$`ten_year_beta`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`security`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`security`) && ((length(self$`security`) == 0) || ((length(self$`security`) != 0 && R6::is.R6(self$`security`[[1]]))))) {
          BetaObject[['security']] <- lapply(self$`security`, function(x) x$toJSON())
        } else {
          BetaObject[['security']] <- jsonlite::toJSON(self$`security`, auto_unbox = TRUE)
        }
      }

      BetaObject
    },
    fromJSON = function(BetaJson) {
      BetaObject <- jsonlite::fromJSON(BetaJson)
      if (!is.null(BetaObject$`period_type`)) {
        self$`period_type` <- BetaObject$`period_type`
      }
      if (!is.null(BetaObject$`one_year_beta`)) {
        self$`one_year_beta` <- BetaObject$`one_year_beta`
      }
      if (!is.null(BetaObject$`three_year_beta`)) {
        self$`three_year_beta` <- BetaObject$`three_year_beta`
      }
      if (!is.null(BetaObject$`five_year_beta`)) {
        self$`five_year_beta` <- BetaObject$`five_year_beta`
      }
      if (!is.null(BetaObject$`seven_year_beta`)) {
        self$`seven_year_beta` <- BetaObject$`seven_year_beta`
      }
      if (!is.null(BetaObject$`ten_year_beta`)) {
        self$`ten_year_beta` <- BetaObject$`ten_year_beta`
      }
      if (!is.null(BetaObject$`security`)) {
        self$`security` <- BetaObject$`security`
      }
    },
    toJSONString = function() {
      jsonlite::toJSON(self$toJSON(), auto_unbox = TRUE, pretty = TRUE)
    },
    fromJSONString = function(BetaJson) {
      BetaObject <- jsonlite::fromJSON(BetaJson, simplifyDataFrame = FALSE)
      self$setFromList(BetaObject)
    },
    setFromList = function(listObject) {
      if (!is.null(listObject$`period_type`)) {
        self$`period_type` <- listObject$`period_type`
      }
      else {
        self$`period_type` <- NA 
      }

      if (!is.null(listObject$`one_year_beta`)) {
        self$`one_year_beta` <- listObject$`one_year_beta`
      }
      else {
        self$`one_year_beta` <- NA 
      }

      if (!is.null(listObject$`three_year_beta`)) {
        self$`three_year_beta` <- listObject$`three_year_beta`
      }
      else {
        self$`three_year_beta` <- NA 
      }

      if (!is.null(listObject$`five_year_beta`)) {
        self$`five_year_beta` <- listObject$`five_year_beta`
      }
      else {
        self$`five_year_beta` <- NA 
      }

      if (!is.null(listObject$`seven_year_beta`)) {
        self$`seven_year_beta` <- listObject$`seven_year_beta`
      }
      else {
        self$`seven_year_beta` <- NA 
      }

      if (!is.null(listObject$`ten_year_beta`)) {
        self$`ten_year_beta` <- listObject$`ten_year_beta`
      }
      else {
        self$`ten_year_beta` <- NA 
      }








      self$`security` <- SecuritySummary$new()
      self$`security`$setFromList(listObject$`security`)

    },
    getAsList = function() {
      listObject = list()
      listObject[["period_type"]] <- self$`period_type`
      listObject[["one_year_beta"]] <- self$`one_year_beta`
      listObject[["three_year_beta"]] <- self$`three_year_beta`
      listObject[["five_year_beta"]] <- self$`five_year_beta`
      listObject[["seven_year_beta"]] <- self$`seven_year_beta`
      listObject[["ten_year_beta"]] <- self$`ten_year_beta`




      security_list <- self$`security`$getAsList()
      for (x in names(security_list)) {
        listObject[[paste("security_",x, sep = "")]] <- self$`security`[[x]]
      }
        
      return(listObject)
    }
  )
)
