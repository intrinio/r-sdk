# Intrinio API
#
# Welcome to the Intrinio API! Through our Financial Data Marketplace, we offer a wide selection of financial data feed APIs sourced by our own proprietary processes as well as from many data vendors. For a complete API request / response reference please view the [Intrinio API documentation](https://docs.intrinio.com/documentation/api_v2). If you need additional help in using the API, please visit the [Intrinio website](https://intrinio.com) and click on the chat icon in the lower right corner.
#
# OpenAPI spec version: 2.108.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' ShortInterestWithSecurity Class
#'
#' @field settlement_date 
#' @field current_short_position 
#' @field previous_short_position 
#' @field change_percent 
#' @field days_to_cover 
#' @field average_daily_volume 
#' @field security 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ShortInterestWithSecurity <- R6::R6Class(
  'ShortInterestWithSecurity',
  public = list(
    `settlement_date` = NA,
    `current_short_position` = NA,
    `previous_short_position` = NA,
    `change_percent` = NA,
    `days_to_cover` = NA,
    `average_daily_volume` = NA,
    `security` = NA,
    initialize = function(`settlement_date`, `current_short_position`, `previous_short_position`, `change_percent`, `days_to_cover`, `average_daily_volume`, `security`){
      if (!missing(`settlement_date`)) {
        self$`settlement_date` <- `settlement_date`
      }
      if (!missing(`current_short_position`)) {
        self$`current_short_position` <- `current_short_position`
      }
      if (!missing(`previous_short_position`)) {
        self$`previous_short_position` <- `previous_short_position`
      }
      if (!missing(`change_percent`)) {
        self$`change_percent` <- `change_percent`
      }
      if (!missing(`days_to_cover`)) {
        self$`days_to_cover` <- `days_to_cover`
      }
      if (!missing(`average_daily_volume`)) {
        self$`average_daily_volume` <- `average_daily_volume`
      }
      if (!missing(`security`)) {
        self$`security` <- `security`
      }
    },
    toJSON = function() {
      ShortInterestWithSecurityObject <- list()
      if (!is.null(self$`settlement_date`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`settlement_date`) && ((length(self$`settlement_date`) == 0) || ((length(self$`settlement_date`) != 0 && R6::is.R6(self$`settlement_date`[[1]]))))) {
          ShortInterestWithSecurityObject[['settlement_date']] <- lapply(self$`settlement_date`, function(x) x$toJSON())
        } else {
          ShortInterestWithSecurityObject[['settlement_date']] <- jsonlite::toJSON(self$`settlement_date`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`current_short_position`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`current_short_position`) && ((length(self$`current_short_position`) == 0) || ((length(self$`current_short_position`) != 0 && R6::is.R6(self$`current_short_position`[[1]]))))) {
          ShortInterestWithSecurityObject[['current_short_position']] <- lapply(self$`current_short_position`, function(x) x$toJSON())
        } else {
          ShortInterestWithSecurityObject[['current_short_position']] <- jsonlite::toJSON(self$`current_short_position`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`previous_short_position`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`previous_short_position`) && ((length(self$`previous_short_position`) == 0) || ((length(self$`previous_short_position`) != 0 && R6::is.R6(self$`previous_short_position`[[1]]))))) {
          ShortInterestWithSecurityObject[['previous_short_position']] <- lapply(self$`previous_short_position`, function(x) x$toJSON())
        } else {
          ShortInterestWithSecurityObject[['previous_short_position']] <- jsonlite::toJSON(self$`previous_short_position`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`change_percent`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`change_percent`) && ((length(self$`change_percent`) == 0) || ((length(self$`change_percent`) != 0 && R6::is.R6(self$`change_percent`[[1]]))))) {
          ShortInterestWithSecurityObject[['change_percent']] <- lapply(self$`change_percent`, function(x) x$toJSON())
        } else {
          ShortInterestWithSecurityObject[['change_percent']] <- jsonlite::toJSON(self$`change_percent`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`days_to_cover`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`days_to_cover`) && ((length(self$`days_to_cover`) == 0) || ((length(self$`days_to_cover`) != 0 && R6::is.R6(self$`days_to_cover`[[1]]))))) {
          ShortInterestWithSecurityObject[['days_to_cover']] <- lapply(self$`days_to_cover`, function(x) x$toJSON())
        } else {
          ShortInterestWithSecurityObject[['days_to_cover']] <- jsonlite::toJSON(self$`days_to_cover`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`average_daily_volume`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`average_daily_volume`) && ((length(self$`average_daily_volume`) == 0) || ((length(self$`average_daily_volume`) != 0 && R6::is.R6(self$`average_daily_volume`[[1]]))))) {
          ShortInterestWithSecurityObject[['average_daily_volume']] <- lapply(self$`average_daily_volume`, function(x) x$toJSON())
        } else {
          ShortInterestWithSecurityObject[['average_daily_volume']] <- jsonlite::toJSON(self$`average_daily_volume`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`security`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`security`) && ((length(self$`security`) == 0) || ((length(self$`security`) != 0 && R6::is.R6(self$`security`[[1]]))))) {
          ShortInterestWithSecurityObject[['security']] <- lapply(self$`security`, function(x) x$toJSON())
        } else {
          ShortInterestWithSecurityObject[['security']] <- jsonlite::toJSON(self$`security`, auto_unbox = TRUE)
        }
      }

      ShortInterestWithSecurityObject
    },
    fromJSON = function(ShortInterestWithSecurityJson) {
      ShortInterestWithSecurityObject <- jsonlite::fromJSON(ShortInterestWithSecurityJson)
      if (!is.null(ShortInterestWithSecurityObject$`settlement_date`)) {
        self$`settlement_date` <- ShortInterestWithSecurityObject$`settlement_date`
      }
      if (!is.null(ShortInterestWithSecurityObject$`current_short_position`)) {
        self$`current_short_position` <- ShortInterestWithSecurityObject$`current_short_position`
      }
      if (!is.null(ShortInterestWithSecurityObject$`previous_short_position`)) {
        self$`previous_short_position` <- ShortInterestWithSecurityObject$`previous_short_position`
      }
      if (!is.null(ShortInterestWithSecurityObject$`change_percent`)) {
        self$`change_percent` <- ShortInterestWithSecurityObject$`change_percent`
      }
      if (!is.null(ShortInterestWithSecurityObject$`days_to_cover`)) {
        self$`days_to_cover` <- ShortInterestWithSecurityObject$`days_to_cover`
      }
      if (!is.null(ShortInterestWithSecurityObject$`average_daily_volume`)) {
        self$`average_daily_volume` <- ShortInterestWithSecurityObject$`average_daily_volume`
      }
      if (!is.null(ShortInterestWithSecurityObject$`security`)) {
        self$`security` <- ShortInterestWithSecurityObject$`security`
      }
    },
    toJSONString = function() {
      jsonlite::toJSON(self$toJSON(), auto_unbox = TRUE, pretty = TRUE)
    },
    fromJSONString = function(ShortInterestWithSecurityJson) {
      ShortInterestWithSecurityObject <- jsonlite::fromJSON(ShortInterestWithSecurityJson, simplifyDataFrame = FALSE)
      self$setFromList(ShortInterestWithSecurityObject)
    },
    setFromList = function(listObject) {



      if (!is.null(listObject$`settlement_date`)) {
        self$`settlement_date` <- self$`settlement_date` <- as.Date(listObject$`settlement_date`, "%Y-%m-%d")
      }
      else {
        self$`settlement_date` <- NA 
      }





      if (!is.null(listObject$`current_short_position`)) {
        self$`current_short_position` <- listObject$`current_short_position`
      }
      else {
        self$`current_short_position` <- NA 
      }

      if (!is.null(listObject$`previous_short_position`)) {
        self$`previous_short_position` <- listObject$`previous_short_position`
      }
      else {
        self$`previous_short_position` <- NA 
      }

      if (!is.null(listObject$`change_percent`)) {
        self$`change_percent` <- listObject$`change_percent`
      }
      else {
        self$`change_percent` <- NA 
      }

      if (!is.null(listObject$`days_to_cover`)) {
        self$`days_to_cover` <- listObject$`days_to_cover`
      }
      else {
        self$`days_to_cover` <- NA 
      }

      if (!is.null(listObject$`average_daily_volume`)) {
        self$`average_daily_volume` <- listObject$`average_daily_volume`
      }
      else {
        self$`average_daily_volume` <- NA 
      }








      self$`security` <- SecuritySummary$new()
      self$`security`$setFromList(listObject$`security`)

    },
    getAsList = function() {
      listObject = list()

      listObject[["settlement_date"]] <- self$`settlement_date`



        
      listObject[["current_short_position"]] <- self$`current_short_position`
      listObject[["previous_short_position"]] <- self$`previous_short_position`
      listObject[["change_percent"]] <- self$`change_percent`
      listObject[["days_to_cover"]] <- self$`days_to_cover`
      listObject[["average_daily_volume"]] <- self$`average_daily_volume`




      security_list <- self$`security`$getAsList()
      for (x in names(security_list)) {
        listObject[[paste("security_",x, sep = "")]] <- self$`security`[[x]]
      }
        
      return(listObject)
    }
  )
)
