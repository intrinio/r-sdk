# Intrinio API
#
# Welcome to the Intrinio API! Through our Financial Data Marketplace, we offer a wide selection of financial data feed APIs sourced by our own proprietary processes as well as from many data vendors. For a complete API request / response reference please view the [Intrinio API documentation](https://docs.intrinio.com/documentation/api_v2). If you need additional help in using the API, please visit the [Intrinio website](https://intrinio.com) and click on the chat icon in the lower right corner.
#
# OpenAPI spec version: 2.107.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' OptionTradesResult Class
#'
#' @field next_page 
#' @field source 
#' @field trades 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
OptionTradesResult <- R6::R6Class(
  'OptionTradesResult',
  public = list(
    `next_page` = NA,
    `source` = NA,
    `trades` = NA,
    `trades_data_frame` = NULL,
    initialize = function(`next_page`, `source`, `trades`){
      if (!missing(`next_page`)) {
        self$`next_page` <- `next_page`
      }
      if (!missing(`source`)) {
        self$`source` <- `source`
      }
      if (!missing(`trades`)) {
        self$`trades` <- `trades`
      }
    },
    toJSON = function() {
      OptionTradesResultObject <- list()
      if (!is.null(self$`next_page`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`next_page`) && ((length(self$`next_page`) == 0) || ((length(self$`next_page`) != 0 && R6::is.R6(self$`next_page`[[1]]))))) {
          OptionTradesResultObject[['next_page']] <- lapply(self$`next_page`, function(x) x$toJSON())
        } else {
          OptionTradesResultObject[['next_page']] <- jsonlite::toJSON(self$`next_page`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`source`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`source`) && ((length(self$`source`) == 0) || ((length(self$`source`) != 0 && R6::is.R6(self$`source`[[1]]))))) {
          OptionTradesResultObject[['source']] <- lapply(self$`source`, function(x) x$toJSON())
        } else {
          OptionTradesResultObject[['source']] <- jsonlite::toJSON(self$`source`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`trades`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`trades`) && ((length(self$`trades`) == 0) || ((length(self$`trades`) != 0 && R6::is.R6(self$`trades`[[1]]))))) {
          OptionTradesResultObject[['trades']] <- lapply(self$`trades`, function(x) x$toJSON())
        } else {
          OptionTradesResultObject[['trades']] <- jsonlite::toJSON(self$`trades`, auto_unbox = TRUE)
        }
      }

      OptionTradesResultObject
    },
    fromJSON = function(OptionTradesResultJson) {
      OptionTradesResultObject <- jsonlite::fromJSON(OptionTradesResultJson)
      if (!is.null(OptionTradesResultObject$`next_page`)) {
        self$`next_page` <- OptionTradesResultObject$`next_page`
      }
      if (!is.null(OptionTradesResultObject$`source`)) {
        self$`source` <- OptionTradesResultObject$`source`
      }
      if (!is.null(OptionTradesResultObject$`trades`)) {
        self$`trades` <- OptionTradesResultObject$`trades`
      }
    },
    toJSONString = function() {
      jsonlite::toJSON(self$toJSON(), auto_unbox = TRUE, pretty = TRUE)
    },
    fromJSONString = function(OptionTradesResultJson) {
      OptionTradesResultObject <- jsonlite::fromJSON(OptionTradesResultJson, simplifyDataFrame = FALSE)
      self$setFromList(OptionTradesResultObject)
    },
    setFromList = function(listObject) {
      if (!is.null(listObject$`next_page`)) {
        self$`next_page` <- listObject$`next_page`
      }
      else {
        self$`next_page` <- NA 
      }

      if (!is.null(listObject$`source`)) {
        self$`source` <- listObject$`source`
      }
      else {
        self$`source` <- NA 
      }



      self$`trades` <- lapply(listObject$`trades`, function(x) {
      OptionTradesObject <- OptionTrades$new()
      OptionTradesObject$setFromList(x)
      return(OptionTradesObject)
      })

      trades_list <- lapply(self$`trades`, function(x) {
        return(x$getAsList()) 
      })

      self$`trades_data_frame` <- do.call(rbind, lapply(trades_list, data.frame))






    },
    getAsList = function() {
      listObject = list()
      listObject[["next_page"]] <- self$`next_page`
      listObject[["source"]] <- self$`source`
      # listObject[["trades"]] <- lapply(self$`trades`, function(o) {
      #  return(o$getAsList())
      # })




        
      return(listObject)
    }
  )
)
