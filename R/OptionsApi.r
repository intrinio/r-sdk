# Intrinio API
#
# Welcome to the Intrinio API! Through our Financial Data Marketplace, we offer a wide selection of financial data feed APIs sourced by our own proprietary processes as well as from many data vendors. For a complete API request / response reference please view the [Intrinio API documentation](https://docs.intrinio.com/documentation/api_v2). If you need additional help in using the API, please visit the [Intrinio website](https://intrinio.com) and click on the chat icon in the lower right corner.
#
# OpenAPI spec version: 2.61.1
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' @title Options operations
#' @description IntrinioSDK.Options
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' get_all_options_tickers Options Tickers
#'
#'
#' get_option_aggregates Total open interest and volume aggregated by ticker
#'
#'
#' get_option_expirations_realtime Options Expirations
#'
#'
#' get_option_strikes_realtime Option Strikes Realtime
#'
#'
#' get_options Options
#'
#'
#' get_options_by_symbol_realtime Options by Symbol Realtime
#'
#'
#' get_options_chain Options Chain
#'
#'
#' get_options_chain_eod Options Chain EOD
#'
#'
#' get_options_chain_realtime Options Chain Realtime
#'
#'
#' get_options_expirations Options Expirations
#'
#'
#' get_options_expirations_eod Options Expirations
#'
#'
#' get_options_interval_by_contract Options Intervals By Contract
#'
#'
#' get_options_interval_movers Options Intervals Movers
#'
#'
#' get_options_interval_movers_change Options Intervals Movers By Change
#'
#'
#' get_options_interval_movers_volume Options Intervals Movers By Volume
#'
#'
#' get_options_prices Option Prices
#'
#'
#' get_options_prices_batch_realtime Option Prices Batch Realtime
#'
#'
#' get_options_prices_eod Option Prices EOD
#'
#'
#' get_options_prices_realtime Option Prices Realtime
#'
#'
#' get_options_snapshots Option Prices Realtime Snapshot
#'
#'
#' get_options_stats_realtime Option Stats Realtime
#'
#'
#' get_unusual_activity Options Unusual Activity
#'
#'
#' get_unusual_activity_intraday Options Unusual Activity Intraday
#'
#'
#' get_unusual_activity_universal Options Unusual Activity Universal
#'
#'
#' get_unusual_activity_universal_intraday Options Unusual Activity Universal Intraday
#'
#' }
#'
#' @export
OptionsApi <- R6::R6Class(
  'OptionsApi',
  public = list(
    userAgent = "Swagger-Codegen/1.25.3/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    get_all_options_tickers = function(opts = list()){
      queryParams <- list()
      headerParams <- character()

      urlPath <- "/options/tickers"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsTickers" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsTickers" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsTickers" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsTickers" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsTickers$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_option_aggregates = function(opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('date' %in% names(opts)) {
        queryParams['date'] <- opts['date']
      }
      urlPath <- "/options/aggregates"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsAggregates" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsAggregates" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsAggregates" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsAggregates" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsAggregates$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_option_expirations_realtime = function(symbol, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('after' %in% names(opts)) {
        queryParams['after'] <- opts['after']
      }
      if ('before' %in% names(opts)) {
        queryParams['before'] <- opts['before']
      }
      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('include_related_symbols' %in% names(opts)) {
        queryParams['include_related_symbols'] <- opts['include_related_symbols']
      }
      urlPath <- "/options/expirations/{symbol}/realtime"
      if (!missing(`symbol`)) {
        urlPath <- gsub(paste0("\\{", "symbol", "\\}"), `symbol`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsExpirations" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsExpirations" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsExpirations" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsExpirations" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsExpirations$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_option_strikes_realtime = function(symbol, strike, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('stock_price_source' %in% names(opts)) {
        queryParams['stock_price_source'] <- opts['stock_price_source']
      }
      if ('model' %in% names(opts)) {
        queryParams['model'] <- opts['model']
      }
      if ('show_extended_price' %in% names(opts)) {
        queryParams['show_extended_price'] <- opts['show_extended_price']
      }
      if ('include_related_symbols' %in% names(opts)) {
        queryParams['include_related_symbols'] <- opts['include_related_symbols']
      }
      urlPath <- "/options/strikes/{symbol}/{strike}/realtime"
      if (!missing(`symbol`)) {
        urlPath <- gsub(paste0("\\{", "symbol", "\\}"), `symbol`, urlPath)
      }

      if (!missing(`strike`)) {
        urlPath <- gsub(paste0("\\{", "strike", "\\}"), `strike`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsChainRealtime" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsChainRealtime" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsChainRealtime" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsChainRealtime" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsChainRealtime$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options = function(symbol, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('type' %in% names(opts)) {
        queryParams['type'] <- opts['type']
      }
      if ('strike' %in% names(opts)) {
        queryParams['strike'] <- opts['strike']
      }
      if ('strike_greater_than' %in% names(opts)) {
        queryParams['strike_greater_than'] <- opts['strike_greater_than']
      }
      if ('strike_less_than' %in% names(opts)) {
        queryParams['strike_less_than'] <- opts['strike_less_than']
      }
      if ('expiration' %in% names(opts)) {
        queryParams['expiration'] <- opts['expiration']
      }
      if ('expiration_after' %in% names(opts)) {
        queryParams['expiration_after'] <- opts['expiration_after']
      }
      if ('expiration_before' %in% names(opts)) {
        queryParams['expiration_before'] <- opts['expiration_before']
      }
      if ('page_size' %in% names(opts)) {
        queryParams['page_size'] <- opts['page_size']
      }
      if ('next_page' %in% names(opts)) {
        queryParams['next_page'] <- opts['next_page']
      }
      urlPath <- "/options/{symbol}"
      if (!missing(`symbol`)) {
        urlPath <- gsub(paste0("\\{", "symbol", "\\}"), `symbol`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptions" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptions" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptions" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptions" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptions$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_by_symbol_realtime = function(symbol, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('type' %in% names(opts)) {
        queryParams['type'] <- opts['type']
      }
      if ('strike' %in% names(opts)) {
        queryParams['strike'] <- opts['strike']
      }
      if ('strike_greater_than' %in% names(opts)) {
        queryParams['strike_greater_than'] <- opts['strike_greater_than']
      }
      if ('strike_less_than' %in% names(opts)) {
        queryParams['strike_less_than'] <- opts['strike_less_than']
      }
      if ('expiration' %in% names(opts)) {
        queryParams['expiration'] <- opts['expiration']
      }
      if ('expiration_after' %in% names(opts)) {
        queryParams['expiration_after'] <- opts['expiration_after']
      }
      if ('expiration_before' %in% names(opts)) {
        queryParams['expiration_before'] <- opts['expiration_before']
      }
      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('include_related_symbols' %in% names(opts)) {
        queryParams['include_related_symbols'] <- opts['include_related_symbols']
      }
      urlPath <- "/options/{symbol}/realtime"
      if (!missing(`symbol`)) {
        urlPath <- gsub(paste0("\\{", "symbol", "\\}"), `symbol`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsRealtime" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsRealtime" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsRealtime" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsRealtime" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsRealtime$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_chain = function(symbol, expiration, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('date' %in% names(opts)) {
        queryParams['date'] <- opts['date']
      }
      if ('type' %in% names(opts)) {
        queryParams['type'] <- opts['type']
      }
      if ('strike' %in% names(opts)) {
        queryParams['strike'] <- opts['strike']
      }
      if ('strike_greater_than' %in% names(opts)) {
        queryParams['strike_greater_than'] <- opts['strike_greater_than']
      }
      if ('strike_less_than' %in% names(opts)) {
        queryParams['strike_less_than'] <- opts['strike_less_than']
      }
      if ('moneyness' %in% names(opts)) {
        queryParams['moneyness'] <- opts['moneyness']
      }
      if ('page_size' %in% names(opts)) {
        queryParams['page_size'] <- opts['page_size']
      }
      urlPath <- "/options/chain/{symbol}/{expiration}"
      if (!missing(`symbol`)) {
        urlPath <- gsub(paste0("\\{", "symbol", "\\}"), `symbol`, urlPath)
      }

      if (!missing(`expiration`)) {
        urlPath <- gsub(paste0("\\{", "expiration", "\\}"), `expiration`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsChain" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsChain" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsChain" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsChain" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsChain$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_chain_eod = function(symbol, expiration, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('type' %in% names(opts)) {
        queryParams['type'] <- opts['type']
      }
      if ('strike' %in% names(opts)) {
        queryParams['strike'] <- opts['strike']
      }
      if ('strike_greater_than' %in% names(opts)) {
        queryParams['strike_greater_than'] <- opts['strike_greater_than']
      }
      if ('strike_less_than' %in% names(opts)) {
        queryParams['strike_less_than'] <- opts['strike_less_than']
      }
      if ('date' %in% names(opts)) {
        queryParams['date'] <- opts['date']
      }
      if ('include_related_symbols' %in% names(opts)) {
        queryParams['include_related_symbols'] <- opts['include_related_symbols']
      }
      urlPath <- "/options/chain/{symbol}/{expiration}/eod"
      if (!missing(`symbol`)) {
        urlPath <- gsub(paste0("\\{", "symbol", "\\}"), `symbol`, urlPath)
      }

      if (!missing(`expiration`)) {
        urlPath <- gsub(paste0("\\{", "expiration", "\\}"), `expiration`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsChainEod" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsChainEod" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsChainEod" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsChainEod" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsChainEod$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_chain_realtime = function(symbol, expiration, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('type' %in% names(opts)) {
        queryParams['type'] <- opts['type']
      }
      if ('strike' %in% names(opts)) {
        queryParams['strike'] <- opts['strike']
      }
      if ('strike_greater_than' %in% names(opts)) {
        queryParams['strike_greater_than'] <- opts['strike_greater_than']
      }
      if ('strike_less_than' %in% names(opts)) {
        queryParams['strike_less_than'] <- opts['strike_less_than']
      }
      if ('volume_greater_than' %in% names(opts)) {
        queryParams['volume_greater_than'] <- opts['volume_greater_than']
      }
      if ('volume_less_than' %in% names(opts)) {
        queryParams['volume_less_than'] <- opts['volume_less_than']
      }
      if ('open_interest_greater_than' %in% names(opts)) {
        queryParams['open_interest_greater_than'] <- opts['open_interest_greater_than']
      }
      if ('open_interest_less_than' %in% names(opts)) {
        queryParams['open_interest_less_than'] <- opts['open_interest_less_than']
      }
      if ('moneyness' %in% names(opts)) {
        queryParams['moneyness'] <- opts['moneyness']
      }
      if ('stock_price_source' %in% names(opts)) {
        queryParams['stock_price_source'] <- opts['stock_price_source']
      }
      if ('model' %in% names(opts)) {
        queryParams['model'] <- opts['model']
      }
      if ('show_extended_price' %in% names(opts)) {
        queryParams['show_extended_price'] <- opts['show_extended_price']
      }
      if ('include_related_symbols' %in% names(opts)) {
        queryParams['include_related_symbols'] <- opts['include_related_symbols']
      }
      urlPath <- "/options/chain/{symbol}/{expiration}/realtime"
      if (!missing(`symbol`)) {
        urlPath <- gsub(paste0("\\{", "symbol", "\\}"), `symbol`, urlPath)
      }

      if (!missing(`expiration`)) {
        urlPath <- gsub(paste0("\\{", "expiration", "\\}"), `expiration`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsChainRealtime" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsChainRealtime" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsChainRealtime" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsChainRealtime" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsChainRealtime$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_expirations = function(symbol, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('after' %in% names(opts)) {
        queryParams['after'] <- opts['after']
      }
      if ('before' %in% names(opts)) {
        queryParams['before'] <- opts['before']
      }
      urlPath <- "/options/expirations/{symbol}"
      if (!missing(`symbol`)) {
        urlPath <- gsub(paste0("\\{", "symbol", "\\}"), `symbol`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsExpirations" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsExpirations" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsExpirations" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsExpirations" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsExpirations$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_expirations_eod = function(symbol, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('after' %in% names(opts)) {
        queryParams['after'] <- opts['after']
      }
      if ('before' %in% names(opts)) {
        queryParams['before'] <- opts['before']
      }
      if ('include_related_symbols' %in% names(opts)) {
        queryParams['include_related_symbols'] <- opts['include_related_symbols']
      }
      urlPath <- "/options/expirations/{symbol}/eod"
      if (!missing(`symbol`)) {
        urlPath <- gsub(paste0("\\{", "symbol", "\\}"), `symbol`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsExpirations" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsExpirations" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsExpirations" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsExpirations" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsExpirations$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_interval_by_contract = function(identifier, interval_size, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if (!missing(`interval_size`)) {
        queryParams['interval_size'] <- `interval_size`
      }
      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('page_size' %in% names(opts)) {
        queryParams['page_size'] <- opts['page_size']
      }
      if ('end_time' %in% names(opts)) {
        queryParams['end_time'] <- opts['end_time']
      }
      urlPath <- "/options/interval/{identifier}"
      if (!missing(`identifier`)) {
        urlPath <- gsub(paste0("\\{", "identifier", "\\}"), `identifier`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("OptionIntervalsResult" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("OptionIntervalsResult" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("OptionIntervalsResult" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("OptionIntervalsResult" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- OptionIntervalsResult$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_interval_movers = function(opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('open_time' %in% names(opts)) {
        queryParams['open_time'] <- opts['open_time']
      }
      urlPath <- "/options/interval/movers"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("OptionIntervalsMoversResult" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("OptionIntervalsMoversResult" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("OptionIntervalsMoversResult" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("OptionIntervalsMoversResult" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- OptionIntervalsMoversResult$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_interval_movers_change = function(opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('open_time' %in% names(opts)) {
        queryParams['open_time'] <- opts['open_time']
      }
      urlPath <- "/options/interval/movers/change"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("OptionIntervalsMoversResult" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("OptionIntervalsMoversResult" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("OptionIntervalsMoversResult" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("OptionIntervalsMoversResult" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- OptionIntervalsMoversResult$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_interval_movers_volume = function(opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('open_time' %in% names(opts)) {
        queryParams['open_time'] <- opts['open_time']
      }
      urlPath <- "/options/interval/movers/volume"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("OptionIntervalsMoversResult" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("OptionIntervalsMoversResult" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("OptionIntervalsMoversResult" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("OptionIntervalsMoversResult" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- OptionIntervalsMoversResult$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_prices = function(identifier, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('start_date' %in% names(opts)) {
        queryParams['start_date'] <- opts['start_date']
      }
      if ('end_date' %in% names(opts)) {
        queryParams['end_date'] <- opts['end_date']
      }
      if ('page_size' %in% names(opts)) {
        queryParams['page_size'] <- opts['page_size']
      }
      if ('next_page' %in% names(opts)) {
        queryParams['next_page'] <- opts['next_page']
      }
      urlPath <- "/options/prices/{identifier}"
      if (!missing(`identifier`)) {
        urlPath <- gsub(paste0("\\{", "identifier", "\\}"), `identifier`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionPrices" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionPrices" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionPrices" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionPrices" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionPrices$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_prices_batch_realtime = function(body, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('show_stats' %in% names(opts)) {
        queryParams['show_stats'] <- opts['show_stats']
      }
      if ('stock_price_source' %in% names(opts)) {
        queryParams['stock_price_source'] <- opts['stock_price_source']
      }
      if ('model' %in% names(opts)) {
        queryParams['model'] <- opts['model']
      }
      if ('show_extended_price' %in% names(opts)) {
        queryParams['show_extended_price'] <- opts['show_extended_price']
      }
      if (!is.null(body)) {
        body <- jsonlite::toJSON(body)
        body <- gsub("\\\\\"", "", body)
      } else if ('body' %in% names(opts)) {
        body <- jsonlite::toJSON(opts['body']$body$toJSON(), auto_unbox = TRUE)
        body <- gsub("\\\\\"", "", body)
      } else {
        body <- NULL
      }

      urlPath <- "/options/prices/realtime/batch"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsPricesBatchRealtime" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsPricesBatchRealtime" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsPricesBatchRealtime" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsPricesBatchRealtime" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsPricesBatchRealtime$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_prices_eod = function(identifier, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('next_page' %in% names(opts)) {
        queryParams['next_page'] <- opts['next_page']
      }
      if ('start_date' %in% names(opts)) {
        queryParams['start_date'] <- opts['start_date']
      }
      if ('end_date' %in% names(opts)) {
        queryParams['end_date'] <- opts['end_date']
      }
      urlPath <- "/options/prices/{identifier}/eod"
      if (!missing(`identifier`)) {
        urlPath <- gsub(paste0("\\{", "identifier", "\\}"), `identifier`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsPricesEod" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsPricesEod" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsPricesEod" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsPricesEod" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsPricesEod$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_prices_realtime = function(identifier, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('stock_price_source' %in% names(opts)) {
        queryParams['stock_price_source'] <- opts['stock_price_source']
      }
      if ('model' %in% names(opts)) {
        queryParams['model'] <- opts['model']
      }
      if ('show_extended_price' %in% names(opts)) {
        queryParams['show_extended_price'] <- opts['show_extended_price']
      }
      urlPath <- "/options/prices/{identifier}/realtime"
      if (!missing(`identifier`)) {
        urlPath <- gsub(paste0("\\{", "identifier", "\\}"), `identifier`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsPriceRealtime" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsPriceRealtime" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsPriceRealtime" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsPriceRealtime" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsPriceRealtime$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_snapshots = function(opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('at_datetime' %in% names(opts)) {
        queryParams['at_datetime'] <- opts['at_datetime']
      }
      urlPath <- "/options/snapshots"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("OptionSnapshotsResult" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("OptionSnapshotsResult" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("OptionSnapshotsResult" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("OptionSnapshotsResult" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- OptionSnapshotsResult$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_options_stats_realtime = function(identifier, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      if ('show_extended_price' %in% names(opts)) {
        queryParams['show_extended_price'] <- opts['show_extended_price']
      }
      urlPath <- "/options/prices/{identifier}/realtime/stats"
      if (!missing(`identifier`)) {
        urlPath <- gsub(paste0("\\{", "identifier", "\\}"), `identifier`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsStatsRealtime" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsStatsRealtime" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsStatsRealtime" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsStatsRealtime" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsStatsRealtime$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_unusual_activity = function(symbol, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      urlPath <- "/options/unusual_activity/{symbol}"
      if (!missing(`symbol`)) {
        urlPath <- gsub(paste0("\\{", "symbol", "\\}"), `symbol`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsUnusualActivity" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsUnusualActivity" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsUnusualActivity" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsUnusualActivity" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsUnusualActivity$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_unusual_activity_intraday = function(symbol, opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('next_page' %in% names(opts)) {
        queryParams['next_page'] <- opts['next_page']
      }
      if ('page_size' %in% names(opts)) {
        queryParams['page_size'] <- opts['page_size']
      }
      if ('activity_type' %in% names(opts)) {
        queryParams['activity_type'] <- opts['activity_type']
      }
      if ('sentiment' %in% names(opts)) {
        queryParams['sentiment'] <- opts['sentiment']
      }
      if ('start_date' %in% names(opts)) {
        queryParams['start_date'] <- opts['start_date']
      }
      if ('end_date' %in% names(opts)) {
        queryParams['end_date'] <- opts['end_date']
      }
      if ('minimum_total_value' %in% names(opts)) {
        queryParams['minimum_total_value'] <- opts['minimum_total_value']
      }
      if ('maximum_total_value' %in% names(opts)) {
        queryParams['maximum_total_value'] <- opts['maximum_total_value']
      }
      urlPath <- "/options/unusual_activity/{symbol}/intraday"
      if (!missing(`symbol`)) {
        urlPath <- gsub(paste0("\\{", "symbol", "\\}"), `symbol`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsUnusualActivity" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsUnusualActivity" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsUnusualActivity" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsUnusualActivity" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsUnusualActivity$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_unusual_activity_universal = function(opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('source' %in% names(opts)) {
        queryParams['source'] <- opts['source']
      }
      urlPath <- "/options/unusual_activity"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsUnusualActivity" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsUnusualActivity" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsUnusualActivity" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsUnusualActivity" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsUnusualActivity$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    },
    get_unusual_activity_universal_intraday = function(opts = list()){
      queryParams <- list()
      headerParams <- character()

      if ('next_page' %in% names(opts)) {
        queryParams['next_page'] <- opts['next_page']
      }
      if ('page_size' %in% names(opts)) {
        queryParams['page_size'] <- opts['page_size']
      }
      if ('activity_type' %in% names(opts)) {
        queryParams['activity_type'] <- opts['activity_type']
      }
      if ('sentiment' %in% names(opts)) {
        queryParams['sentiment'] <- opts['sentiment']
      }
      if ('start_date' %in% names(opts)) {
        queryParams['start_date'] <- opts['start_date']
      }
      if ('end_date' %in% names(opts)) {
        queryParams['end_date'] <- opts['end_date']
      }
      if ('minimum_total_value' %in% names(opts)) {
        queryParams['minimum_total_value'] <- opts['minimum_total_value']
      }
      if ('maximum_total_value' %in% names(opts)) {
        queryParams['maximum_total_value'] <- opts['maximum_total_value']
      }
      urlPath <- "/options/unusual_activity/intraday"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        result <- httr::content(resp, "text", encoding = "UTF-8")

        if ("ApiResponseOptionsUnusualActivity" == "Numeric") {
          returnObject <- as.numeric(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsUnusualActivity" == "Integer") {
          returnObject <- as.integer(jsonlite::fromJSON(result))
        } else if ("ApiResponseOptionsUnusualActivity" == "Character") {
          returnObject <- gsub("\\\"", "", result)
        } else if ("ApiResponseOptionsUnusualActivity" == "Logical") {
          returnObject <- as.logical(jsonlite::fromJSON(result))
        } else {
          returnObject <- ApiResponseOptionsUnusualActivity$new()
          returnObject$fromJSONString(result)
        }

        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        result <- jsonlite::fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(result, resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        result <- httr::content(resp, "text", encoding = "UTF-8")
        Response$new(result, resp)
      }

    }
  )
)
