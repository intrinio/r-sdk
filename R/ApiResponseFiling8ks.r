# Intrinio API
#
# Welcome to the Intrinio API! Through our Financial Data Marketplace, we offer a wide selection of financial data feed APIs sourced by our own proprietary processes as well as from many data vendors. For a complete API request / response reference please view the [Intrinio API documentation](https://intrinio.com/documentation/api_v2). If you need additional help in using the API, please visit the [Intrinio website](https://intrinio.com) and click on the chat icon in the lower right corner.
#
# OpenAPI spec version: 2.9.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' ApiResponseFiling8ks Class
#'
#' @field filings 
#' @field next_page 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApiResponseFiling8ks <- R6::R6Class(
  'ApiResponseFiling8ks',
  public = list(
    `filings` = NULL,
    `next_page` = NULL,
    initialize = function(`filings`, `next_page`){
      if (!missing(`filings`)) {
        stopifnot(is.list(`filings`))
        lapply(`filings`, function(x) stopifnot(R6::is.R6(x)))
        self$`filings` <- `filings`
      }
      if (!missing(`next_page`)) {
        stopifnot(is.character(`next_page`), length(`next_page`) == 1)
        self$`next_page` <- `next_page`
      }
    },
    toJSON = function() {
      ApiResponseFiling8ksObject <- list()
      if (!is.null(self$`filings`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`filings`) && ((length(self$`filings`) == 0) || ((length(self$`filings`) != 0 && R6::is.R6(self$`filings`[[1]]))))) {
          ApiResponseFiling8ksObject[['filings']] <- lapply(self$`filings`, function(x) x$toJSON())
        } else {
          ApiResponseFiling8ksObject[['filings']] <- jsonlite::toJSON(self$`filings`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`next_page`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`next_page`) && ((length(self$`next_page`) == 0) || ((length(self$`next_page`) != 0 && R6::is.R6(self$`next_page`[[1]]))))) {
          ApiResponseFiling8ksObject[['next_page']] <- lapply(self$`next_page`, function(x) x$toJSON())
        } else {
          ApiResponseFiling8ksObject[['next_page']] <- jsonlite::toJSON(self$`next_page`, auto_unbox = TRUE)
        }
      }

      ApiResponseFiling8ksObject
    },
    fromJSON = function(ApiResponseFiling8ksJson) {
      ApiResponseFiling8ksObject <- jsonlite::fromJSON(ApiResponseFiling8ksJson)
      if (!is.null(ApiResponseFiling8ksObject$`filings`)) {
        self$`filings` <- ApiResponseFiling8ksObject$`filings`
      }
      if (!is.null(ApiResponseFiling8ksObject$`next_page`)) {
        self$`next_page` <- ApiResponseFiling8ksObject$`next_page`
      }
    },
    toJSONString = function() {
      jsonlite::toJSON(self$toJSON(), auto_unbox = TRUE, pretty = TRUE)
    },
    fromJSONString = function(ApiResponseFiling8ksJson) {
      ApiResponseFiling8ksObject <- jsonlite::fromJSON(ApiResponseFiling8ksJson)
      self$`filings` <- lapply(ApiResponseFiling8ksObject$`filings`, function(x) Filing8k$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`next_page` <- ApiResponseFiling8ksObject$`next_page`
    }
  )
)
