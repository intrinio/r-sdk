# Intrinio API
#
# Welcome to the Intrinio API! Through our Financial Data Marketplace, we offer a wide selection of financial data feed APIs sourced by our own proprietary processes as well as from many data vendors. For a complete API request / response reference please view the [Intrinio API documentation](https://intrinio.com/documentation/api_v2). If you need additional help in using the API, please visit the [Intrinio website](https://intrinio.com) and click on the chat icon in the lower right corner.
#
# OpenAPI spec version: 2.9.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' Filing8kExhibit Class
#'
#' @field filing_url 
#' @field content 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
Filing8kExhibit <- R6::R6Class(
  'Filing8kExhibit',
  public = list(
    `filing_url` = NULL,
    `content` = NULL,
    initialize = function(`filing_url`, `content`){
      if (!missing(`filing_url`)) {
        stopifnot(is.character(`filing_url`), length(`filing_url`) == 1)
        self$`filing_url` <- `filing_url`
      }
      if (!missing(`content`)) {
        stopifnot(R6::is.R6(`content`))
        self$`content` <- `content`
      }
    },
    toJSON = function() {
      Filing8kExhibitObject <- list()
      if (!is.null(self$`filing_url`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`filing_url`) && ((length(self$`filing_url`) == 0) || ((length(self$`filing_url`) != 0 && R6::is.R6(self$`filing_url`[[1]]))))) {
          Filing8kExhibitObject[['filing_url']] <- lapply(self$`filing_url`, function(x) x$toJSON())
        } else {
          Filing8kExhibitObject[['filing_url']] <- jsonlite::toJSON(self$`filing_url`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`content`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`content`) && ((length(self$`content`) == 0) || ((length(self$`content`) != 0 && R6::is.R6(self$`content`[[1]]))))) {
          Filing8kExhibitObject[['content']] <- lapply(self$`content`, function(x) x$toJSON())
        } else {
          Filing8kExhibitObject[['content']] <- jsonlite::toJSON(self$`content`, auto_unbox = TRUE)
        }
      }

      Filing8kExhibitObject
    },
    fromJSON = function(Filing8kExhibitJson) {
      Filing8kExhibitObject <- jsonlite::fromJSON(Filing8kExhibitJson)
      if (!is.null(Filing8kExhibitObject$`filing_url`)) {
        self$`filing_url` <- Filing8kExhibitObject$`filing_url`
      }
      if (!is.null(Filing8kExhibitObject$`content`)) {
        self$`content` <- Filing8kExhibitObject$`content`
      }
    },
    toJSONString = function() {
      jsonlite::toJSON(self$toJSON(), auto_unbox = TRUE, pretty = TRUE)
    },
    fromJSONString = function(Filing8kExhibitJson) {
      Filing8kExhibitObject <- jsonlite::fromJSON(Filing8kExhibitJson)
      self$`filing_url` <- Filing8kExhibitObject$`filing_url`
      Filing8kContentObject <- Filing8kContent$new()
      self$`content` <- Filing8kContentObject$fromJSON(jsonlite::toJSON(Filing8kExhibitObject$content, auto_unbox = TRUE))
    }
  )
)
