# Intrinio API
#
# Welcome to the Intrinio API! Through our Financial Data Marketplace, we offer a wide selection of financial data feed APIs sourced by our own proprietary processes as well as from many data vendors. For a complete API request / response reference please view the [Intrinio API documentation](https://docs.intrinio.com/documentation/api_v2). If you need additional help in using the API, please visit the [Intrinio website](https://intrinio.com) and click on the chat icon in the lower right corner.
#
# OpenAPI spec version: 2.108.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' ShortInterest Class
#'
#' @field settlement_date 
#' @field current_short_position 
#' @field previous_short_position 
#' @field change_percent 
#' @field days_to_cover 
#' @field average_daily_volume 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ShortInterest <- R6::R6Class(
  'ShortInterest',
  public = list(
    `settlement_date` = NA,
    `current_short_position` = NA,
    `previous_short_position` = NA,
    `change_percent` = NA,
    `days_to_cover` = NA,
    `average_daily_volume` = NA,
    initialize = function(`settlement_date`, `current_short_position`, `previous_short_position`, `change_percent`, `days_to_cover`, `average_daily_volume`){
      if (!missing(`settlement_date`)) {
        self$`settlement_date` <- `settlement_date`
      }
      if (!missing(`current_short_position`)) {
        self$`current_short_position` <- `current_short_position`
      }
      if (!missing(`previous_short_position`)) {
        self$`previous_short_position` <- `previous_short_position`
      }
      if (!missing(`change_percent`)) {
        self$`change_percent` <- `change_percent`
      }
      if (!missing(`days_to_cover`)) {
        self$`days_to_cover` <- `days_to_cover`
      }
      if (!missing(`average_daily_volume`)) {
        self$`average_daily_volume` <- `average_daily_volume`
      }
    },
    toJSON = function() {
      ShortInterestObject <- list()
      if (!is.null(self$`settlement_date`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`settlement_date`) && ((length(self$`settlement_date`) == 0) || ((length(self$`settlement_date`) != 0 && R6::is.R6(self$`settlement_date`[[1]]))))) {
          ShortInterestObject[['settlement_date']] <- lapply(self$`settlement_date`, function(x) x$toJSON())
        } else {
          ShortInterestObject[['settlement_date']] <- jsonlite::toJSON(self$`settlement_date`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`current_short_position`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`current_short_position`) && ((length(self$`current_short_position`) == 0) || ((length(self$`current_short_position`) != 0 && R6::is.R6(self$`current_short_position`[[1]]))))) {
          ShortInterestObject[['current_short_position']] <- lapply(self$`current_short_position`, function(x) x$toJSON())
        } else {
          ShortInterestObject[['current_short_position']] <- jsonlite::toJSON(self$`current_short_position`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`previous_short_position`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`previous_short_position`) && ((length(self$`previous_short_position`) == 0) || ((length(self$`previous_short_position`) != 0 && R6::is.R6(self$`previous_short_position`[[1]]))))) {
          ShortInterestObject[['previous_short_position']] <- lapply(self$`previous_short_position`, function(x) x$toJSON())
        } else {
          ShortInterestObject[['previous_short_position']] <- jsonlite::toJSON(self$`previous_short_position`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`change_percent`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`change_percent`) && ((length(self$`change_percent`) == 0) || ((length(self$`change_percent`) != 0 && R6::is.R6(self$`change_percent`[[1]]))))) {
          ShortInterestObject[['change_percent']] <- lapply(self$`change_percent`, function(x) x$toJSON())
        } else {
          ShortInterestObject[['change_percent']] <- jsonlite::toJSON(self$`change_percent`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`days_to_cover`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`days_to_cover`) && ((length(self$`days_to_cover`) == 0) || ((length(self$`days_to_cover`) != 0 && R6::is.R6(self$`days_to_cover`[[1]]))))) {
          ShortInterestObject[['days_to_cover']] <- lapply(self$`days_to_cover`, function(x) x$toJSON())
        } else {
          ShortInterestObject[['days_to_cover']] <- jsonlite::toJSON(self$`days_to_cover`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`average_daily_volume`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`average_daily_volume`) && ((length(self$`average_daily_volume`) == 0) || ((length(self$`average_daily_volume`) != 0 && R6::is.R6(self$`average_daily_volume`[[1]]))))) {
          ShortInterestObject[['average_daily_volume']] <- lapply(self$`average_daily_volume`, function(x) x$toJSON())
        } else {
          ShortInterestObject[['average_daily_volume']] <- jsonlite::toJSON(self$`average_daily_volume`, auto_unbox = TRUE)
        }
      }

      ShortInterestObject
    },
    fromJSON = function(ShortInterestJson) {
      ShortInterestObject <- jsonlite::fromJSON(ShortInterestJson)
      if (!is.null(ShortInterestObject$`settlement_date`)) {
        self$`settlement_date` <- ShortInterestObject$`settlement_date`
      }
      if (!is.null(ShortInterestObject$`current_short_position`)) {
        self$`current_short_position` <- ShortInterestObject$`current_short_position`
      }
      if (!is.null(ShortInterestObject$`previous_short_position`)) {
        self$`previous_short_position` <- ShortInterestObject$`previous_short_position`
      }
      if (!is.null(ShortInterestObject$`change_percent`)) {
        self$`change_percent` <- ShortInterestObject$`change_percent`
      }
      if (!is.null(ShortInterestObject$`days_to_cover`)) {
        self$`days_to_cover` <- ShortInterestObject$`days_to_cover`
      }
      if (!is.null(ShortInterestObject$`average_daily_volume`)) {
        self$`average_daily_volume` <- ShortInterestObject$`average_daily_volume`
      }
    },
    toJSONString = function() {
      jsonlite::toJSON(self$toJSON(), auto_unbox = TRUE, pretty = TRUE)
    },
    fromJSONString = function(ShortInterestJson) {
      ShortInterestObject <- jsonlite::fromJSON(ShortInterestJson, simplifyDataFrame = FALSE)
      self$setFromList(ShortInterestObject)
    },
    setFromList = function(listObject) {



      if (!is.null(listObject$`settlement_date`)) {
        self$`settlement_date` <- self$`settlement_date` <- as.Date(listObject$`settlement_date`, "%Y-%m-%d")
      }
      else {
        self$`settlement_date` <- NA 
      }





      if (!is.null(listObject$`current_short_position`)) {
        self$`current_short_position` <- listObject$`current_short_position`
      }
      else {
        self$`current_short_position` <- NA 
      }

      if (!is.null(listObject$`previous_short_position`)) {
        self$`previous_short_position` <- listObject$`previous_short_position`
      }
      else {
        self$`previous_short_position` <- NA 
      }

      if (!is.null(listObject$`change_percent`)) {
        self$`change_percent` <- listObject$`change_percent`
      }
      else {
        self$`change_percent` <- NA 
      }

      if (!is.null(listObject$`days_to_cover`)) {
        self$`days_to_cover` <- listObject$`days_to_cover`
      }
      else {
        self$`days_to_cover` <- NA 
      }

      if (!is.null(listObject$`average_daily_volume`)) {
        self$`average_daily_volume` <- listObject$`average_daily_volume`
      }
      else {
        self$`average_daily_volume` <- NA 
      }

    },
    getAsList = function() {
      listObject = list()

      listObject[["settlement_date"]] <- self$`settlement_date`



        
      listObject[["current_short_position"]] <- self$`current_short_position`
      listObject[["previous_short_position"]] <- self$`previous_short_position`
      listObject[["change_percent"]] <- self$`change_percent`
      listObject[["days_to_cover"]] <- self$`days_to_cover`
      listObject[["average_daily_volume"]] <- self$`average_daily_volume`
      return(listObject)
    }
  )
)
