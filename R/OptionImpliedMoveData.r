# Intrinio API
#
# Welcome to the Intrinio API! Through our Financial Data Marketplace, we offer a wide selection of financial data feed APIs sourced by our own proprietary processes as well as from many data vendors. For a complete API request / response reference please view the [Intrinio API documentation](https://docs.intrinio.com/documentation/api_v2). If you need additional help in using the API, please visit the [Intrinio website](https://intrinio.com) and click on the chat icon in the lower right corner.
#
# OpenAPI spec version: 2.99.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' OptionImpliedMoveData Class
#'
#' @field amount 
#' @field percentage 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
OptionImpliedMoveData <- R6::R6Class(
  'OptionImpliedMoveData',
  public = list(
    `amount` = NA,
    `percentage` = NA,
    initialize = function(`amount`, `percentage`){
      if (!missing(`amount`)) {
        self$`amount` <- `amount`
      }
      if (!missing(`percentage`)) {
        self$`percentage` <- `percentage`
      }
    },
    toJSON = function() {
      OptionImpliedMoveDataObject <- list()
      if (!is.null(self$`amount`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`amount`) && ((length(self$`amount`) == 0) || ((length(self$`amount`) != 0 && R6::is.R6(self$`amount`[[1]]))))) {
          OptionImpliedMoveDataObject[['amount']] <- lapply(self$`amount`, function(x) x$toJSON())
        } else {
          OptionImpliedMoveDataObject[['amount']] <- jsonlite::toJSON(self$`amount`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`percentage`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`percentage`) && ((length(self$`percentage`) == 0) || ((length(self$`percentage`) != 0 && R6::is.R6(self$`percentage`[[1]]))))) {
          OptionImpliedMoveDataObject[['percentage']] <- lapply(self$`percentage`, function(x) x$toJSON())
        } else {
          OptionImpliedMoveDataObject[['percentage']] <- jsonlite::toJSON(self$`percentage`, auto_unbox = TRUE)
        }
      }

      OptionImpliedMoveDataObject
    },
    fromJSON = function(OptionImpliedMoveDataJson) {
      OptionImpliedMoveDataObject <- jsonlite::fromJSON(OptionImpliedMoveDataJson)
      if (!is.null(OptionImpliedMoveDataObject$`amount`)) {
        self$`amount` <- OptionImpliedMoveDataObject$`amount`
      }
      if (!is.null(OptionImpliedMoveDataObject$`percentage`)) {
        self$`percentage` <- OptionImpliedMoveDataObject$`percentage`
      }
    },
    toJSONString = function() {
      jsonlite::toJSON(self$toJSON(), auto_unbox = TRUE, pretty = TRUE)
    },
    fromJSONString = function(OptionImpliedMoveDataJson) {
      OptionImpliedMoveDataObject <- jsonlite::fromJSON(OptionImpliedMoveDataJson, simplifyDataFrame = FALSE)
      self$setFromList(OptionImpliedMoveDataObject)
    },
    setFromList = function(listObject) {
      if (!is.null(listObject$`amount`)) {
        self$`amount` <- listObject$`amount`
      }
      else {
        self$`amount` <- NA 
      }

      if (!is.null(listObject$`percentage`)) {
        self$`percentage` <- listObject$`percentage`
      }
      else {
        self$`percentage` <- NA 
      }

    },
    getAsList = function() {
      listObject = list()
      listObject[["amount"]] <- self$`amount`
      listObject[["percentage"]] <- self$`percentage`
      return(listObject)
    }
  )
)
