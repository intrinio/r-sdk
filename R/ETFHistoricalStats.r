# Intrinio API
#
# Welcome to the Intrinio API! Through our Financial Data Marketplace, we offer a wide selection of financial data feed APIs sourced by our own proprietary processes as well as from many data vendors. For a complete API request / response reference please view the [Intrinio API documentation](https://docs.intrinio.com/documentation/api_v2). If you need additional help in using the API, please visit the [Intrinio website](https://intrinio.com) and click on the chat icon in the lower right corner.
#
# OpenAPI spec version: 2.107.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' ETFHistoricalStats Class
#'
#' @field messages 
#' @field etf 
#' @field stats 
#' @field next_page 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ETFHistoricalStats <- R6::R6Class(
  'ETFHistoricalStats',
  public = list(
    `messages` = NA,
    `messages_data_frame` = NULL,
    `etf` = NA,
    `stats` = NA,
    `stats_data_frame` = NULL,
    `next_page` = NA,
    initialize = function(`messages`, `etf`, `stats`, `next_page`){
      if (!missing(`messages`)) {
        self$`messages` <- `messages`
      }
      if (!missing(`etf`)) {
        self$`etf` <- `etf`
      }
      if (!missing(`stats`)) {
        self$`stats` <- `stats`
      }
      if (!missing(`next_page`)) {
        self$`next_page` <- `next_page`
      }
    },
    toJSON = function() {
      ETFHistoricalStatsObject <- list()
      if (!is.null(self$`messages`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`messages`) && ((length(self$`messages`) == 0) || ((length(self$`messages`) != 0 && R6::is.R6(self$`messages`[[1]]))))) {
          ETFHistoricalStatsObject[['messages']] <- lapply(self$`messages`, function(x) x$toJSON())
        } else {
          ETFHistoricalStatsObject[['messages']] <- jsonlite::toJSON(self$`messages`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`etf`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`etf`) && ((length(self$`etf`) == 0) || ((length(self$`etf`) != 0 && R6::is.R6(self$`etf`[[1]]))))) {
          ETFHistoricalStatsObject[['etf']] <- lapply(self$`etf`, function(x) x$toJSON())
        } else {
          ETFHistoricalStatsObject[['etf']] <- jsonlite::toJSON(self$`etf`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`stats`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`stats`) && ((length(self$`stats`) == 0) || ((length(self$`stats`) != 0 && R6::is.R6(self$`stats`[[1]]))))) {
          ETFHistoricalStatsObject[['stats']] <- lapply(self$`stats`, function(x) x$toJSON())
        } else {
          ETFHistoricalStatsObject[['stats']] <- jsonlite::toJSON(self$`stats`, auto_unbox = TRUE)
        }
      }
      if (!is.null(self$`next_page`)) {
        # If the object is an empty list or a list of R6 Objects
        if (is.list(self$`next_page`) && ((length(self$`next_page`) == 0) || ((length(self$`next_page`) != 0 && R6::is.R6(self$`next_page`[[1]]))))) {
          ETFHistoricalStatsObject[['next_page']] <- lapply(self$`next_page`, function(x) x$toJSON())
        } else {
          ETFHistoricalStatsObject[['next_page']] <- jsonlite::toJSON(self$`next_page`, auto_unbox = TRUE)
        }
      }

      ETFHistoricalStatsObject
    },
    fromJSON = function(ETFHistoricalStatsJson) {
      ETFHistoricalStatsObject <- jsonlite::fromJSON(ETFHistoricalStatsJson)
      if (!is.null(ETFHistoricalStatsObject$`messages`)) {
        self$`messages` <- ETFHistoricalStatsObject$`messages`
      }
      if (!is.null(ETFHistoricalStatsObject$`etf`)) {
        self$`etf` <- ETFHistoricalStatsObject$`etf`
      }
      if (!is.null(ETFHistoricalStatsObject$`stats`)) {
        self$`stats` <- ETFHistoricalStatsObject$`stats`
      }
      if (!is.null(ETFHistoricalStatsObject$`next_page`)) {
        self$`next_page` <- ETFHistoricalStatsObject$`next_page`
      }
    },
    toJSONString = function() {
      jsonlite::toJSON(self$toJSON(), auto_unbox = TRUE, pretty = TRUE)
    },
    fromJSONString = function(ETFHistoricalStatsJson) {
      ETFHistoricalStatsObject <- jsonlite::fromJSON(ETFHistoricalStatsJson, simplifyDataFrame = FALSE)
      self$setFromList(ETFHistoricalStatsObject)
    },
    setFromList = function(listObject) {


      self$`messages` <- lapply(listObject$`messages`, function(x) {
      ERRORUNKNOWNObject <- ERRORUNKNOWN$new()
      ERRORUNKNOWNObject$setFromList(x)
      return(ERRORUNKNOWNObject)
      })

      messages_list <- lapply(self$`messages`, function(x) {
        return(x$getAsList()) 
      })

      self$`messages_data_frame` <- do.call(rbind, lapply(messages_list, data.frame))













      self$`etf` <- ETFSummary$new()
      self$`etf`$setFromList(listObject$`etf`)



      self$`stats` <- lapply(listObject$`stats`, function(x) {
      ETFStatsObject <- ETFStats$new()
      ETFStatsObject$setFromList(x)
      return(ETFStatsObject)
      })

      stats_list <- lapply(self$`stats`, function(x) {
        return(x$getAsList()) 
      })

      self$`stats_data_frame` <- do.call(rbind, lapply(stats_list, data.frame))






      if (!is.null(listObject$`next_page`)) {
        self$`next_page` <- listObject$`next_page`
      }
      else {
        self$`next_page` <- NA 
      }

    },
    getAsList = function() {
      listObject = list()
      # listObject[["messages"]] <- lapply(self$`messages`, function(o) {
      #  return(o$getAsList())
      # })




        




      etf_list <- self$`etf`$getAsList()
      for (x in names(etf_list)) {
        listObject[[paste("etf_",x, sep = "")]] <- self$`etf`[[x]]
      }
        
      # listObject[["stats"]] <- lapply(self$`stats`, function(o) {
      #  return(o$getAsList())
      # })




        
      listObject[["next_page"]] <- self$`next_page`
      return(listObject)
    }
  )
)
